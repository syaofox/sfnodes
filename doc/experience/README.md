# 经验归档索引（experience）

> 本目录由原单文件 `doc/experience.md` 拆分（2026-08）：**全局章节号 §N 保持不变**，文内互引（含 AGENTS.md）的 `§N` 由下表定位到具体文件，按需读取避免整档加载。
> 本目录归档全部具体机制与踩坑经验（含节点族专属约定）；主文档 `AGENTS.md` 只保留通用规则并引用本表，不重复收录细节。
> 内容基于当时代码/前端版本（comfyui_frontend_package 1.x，Vue 重构后，版本号会随升级变化，以容器内 `pip show comfyui-frontend-package` 为准，各节另有标注），可能随版本升级过时，使用时结合代码核实。

## 文件与章节映射

| 文件 | 主题 | 章节 |
|---|---|---|
| `platform.md` | 平台机制（ComfyUI 前后端通用） | §1 ComfyUI 后端机制（循环/图展开） · §2 ComfyUI 前端机制（经验总结，含 §11 画布多选宽度对齐、§13 槽点/槽名基线错位全局行为、§14 SF Memory 画布菜单清理 VRAM/RAM、§15 画布菜单聚合 📦 SF Menu、§16 主题令牌层：DOM UI 跟随 Color Palette 明暗、§17 任意节点颜色：node.color/bgcolor 机制与前端调色板限制、§18 节点运行时间显示：execution 事件计时 + 原生 node.badges） · §106 容器内 API 实机调试（免浏览器：HTTP API + docker exec/logs 自测，部署同步与重启边界） |
| `patterns.md` | 横切模式与修复批次 | §3 静态检查脚本经验（AST 对比踩坑） · §4 动态 combo 校验与工作流绑定状态（widget 数据载体） · §17 复刻节点去重：sf_common.js / disk_state.py 公共模块收敛与踩坑 · §26 前端架构治理（2026-08）：工具收敛 / 弹层三件套 / 纯模块边界 · §27 2026-08 健壮性修复批次：表达式防御 / ReDoS 交替型 / 路径净化 / 双端镜像补缺 · §39 COLOR 输入类型被 Vue 前端内置 widget 收编（default 写 hex 不写数组，2026-09） · §40 number widget 整数/小数输入切换（SFNumber 改 options 不换类型 + 输出单槽 any 化 + PERCENT 档移除与输出槽类型/槽名前端随档改型，2026-09） · §41 combo→输出槽类型前端改型（SFConvert Anything：setSlotType 元素替换 + configure 恢复补原件缺陷，2026-09） · §43 灵活 optional schema 与 Any Switch 复刻（`__contains__` 恒 True 放行动态输入 + installDynamicSlots 槽位 + any_pack 着色三件套，2026-09） · §49 固定类型动态多槽 SFConditioningCombine（灵活 schema 类型参数化 + 固定类型免着色 + 按链接数补齐/回收，2026-09） · §50 SFConditioningConcat 多路拼接（slot1=to 逐条保留 + from 各取首条 + 缺 base 抛错 + stub-torch 测试，2026-09） · §52 SFUniversalSlider 万能滑条 Canvas 复刻规范化（去全局补丁/相对导入修正/popup 收敛/RETURN_NAMES 静态折中，2026-09） · §53 SFBooleanSwitch 布尔开关 Canvas 复刻（TOGGLE 常量同源/双击标签/properties 键改名，2026-09） · §54 SFIgnoreGroups 忽略多组编组开关复刻（DOM 面板/三模式/graph.change 单例/轮询清理/锚点组员判定 + 两趟写入，2026-09） · §55 SF 注释便签复刻（原版纯前端架构 1:1/全局补丁守卫/画布菜单入口，2026-09） · §57 纯函数单源收敛与测试桩同步（import 别名零改动/语义差参数化/测试桩三形态，2026-09） · §58 第二批收敛（守卫单源/前缀清洗/原子写盘/kind 参数化 + 删文件联动清单，2026-09） · §68 PointsEditor 底图刷新（跨包解析上游链 + 复用 KJNodes editor.processImage 零执行持久化 + 纯 lib 描述符 + 分段偏移/标注帧感知 seek，2026-09） · §69 手动分段 SeC 管线（单次加载 N 段切片 + 每段首帧标注 + SFMaskBatch 合并 + 男性/精液相减，2026-09） · §70 前端 INT 缺 max 默认 2048（第三方节点数值上限规避：定义阶段抬 spec.max，2026-09） · §79 VHS_LoadVideo 追加 filename 输出（跨包运行时补丁：注册表类对象原地补 + 输出槽末尾追加不破坏连线 + 加载时序有限重试，2026-09） · §84 SFConvert Anything 新增 combo 输出档（原样直通 + COMBO 槽型，前后端校验无碍，2026-09） · §86 Combo 树形折叠误伤（原生分组头 "--x--" 下拉跳过折叠：斜杠标签如 "Krea 2 (Turbo/RAW)" 会误触发，2026-09） · §89 动态槽加载恢复扩展到输出侧（循环节点多状态槽：installConfiguredSlotRecovery 双侧恢复 + loop_flow 接入，2026-09） · §116 OUTPUT_NODE 预览返回（legacy dict 的 ui 只能是 dict，PreviewVideo 必须走 io.NodeOutput + as_dict；报错发生在 execute 返回之后，先查产物是否已落盘，2026-09） |
| `nodes-text.md` | 文本与提示词节点 | §6 SFPromptTags：@tag 展开注入 / Picks 游标 / 全屏编辑器 / 中文与拼音（复刻 Pixaroma Prompt） · §7 SFPauseText：prompt 剪枝闸门（复刻 Pixaroma Pause Text） · §14 SFTextFindReplace：查找替换双端镜像（复刻 Pixaroma Find & Replace） · §15 SFValueDropdown：值下拉与输出点对齐（复刻 Pixaroma Dropdown） · §16 SFPromptReader：PNG/视频元数据提示词恢复（复刻 Pixaroma Prompt Reader） · §18 SFLoadImagesPath 目录切换：两源 + 渐进式浏览 + popup 下拉 · §23 SFPromptList：行号编辑器与 wrap 镜像测量 · §24 SFPromptStack：动态 Prompt 列表与行高拖拽 · §29 SFPromptPreset：十一分类组合预设（正交原则 / 随机机制 / LLM 优化链路） · §36 SFLongTextToList：长文本分割转列表（复刻 ComfyUI_Lam LongTextToList） · §37 SFTextListAffix：列表前后缀（输入列表加前后缀） · §42 SFTextPreset 工作流绑定预设→全局持久化（user/sfnodes/text_presets.json 真源 + 工作流回退兼容 + API 失败降级，2026-09） · §46 SFIDClothingSelector 证件照服装单选（复刻孤海画廊 + 复用 styles JSON 生态 id_ 子集 + 草稿优先 + IMAGE 模板图） · §47 SFCharacterSelect 角色三分镜单选（脸/半身/全身三路独立 IMAGE + character_ JSON 生态 + 新双路由） · §48 SFCharacterSelect v2 角色内多选 batch（破兼容 4→3 路 + 双读兼容 + 首图归一） · §74 SFPauseText 中⇄EN 翻译按钮（LLM API 经 /api/sfnodes/translate 后端代理 + sfnodes.Translate.* 设置 + thinking 仅 DeepSeek） · §102 SFPromptList 高亮"幽灵条"（hl 层内容矮→scrollTop 被钳制，::before 撑高；虚拟化 6px 基线错位，2026-09） · §103 SFPromptList 点选模式（readOnly 原生选择→行区间→自动写 start_index/max_rows + 空白行吸附 + 行号栏拖动多选，2026-09） · §107 SFSpreadsheetOutputList 表格输出列表（复刻 ComfyUI-outputlists-combiner：CSV/TSV/Markdown 按选择器/方向输出 7 路数据列表；stdlib csv 纯逻辑对照上游 pandas 27 例逐例一致，base64 文本解码 + Office 二进制明确报错，2026-09） · §108 SFPromptList 自动 total（前端开关反写所驱动 SFForLoopStart.total；count 输出直连构成依赖环被 validate_inputs 拒绝，默认关防改写故意缩短的循环，2026-09） · §115 SFImagePromptRewriter 图像提示词改写（自写观察者协议纯模块 + 复用 llm_client/sfnodes.LLM.* 设置与 LRU；0–10 有序参考图动态槽，有图=图像编辑自动判定；wh_ratio 元数据不入正文 + 一次有界纠正 + 失败保留原文，2026-09） |
| `nodes-image.md` | 图片 / 遮罩 / latent 节点 | §8 SFPauseImage：快照闸门与预览保存（复刻 Pixaroma Pause Image） · §9 SFPauseMask：遮罩快照闸门（Pixaroma Pause Mask 同构扩展） · §11 SFImageCrop/SFImageUncrop：可视化裁剪与贴回（复刻 Pixaroma Crop/Uncrop） · §12 SFImageOutpaint/Stitch：外绘填充与原始图贴回（复刻 Pixaroma Outpaint） · §13 SFImageResize：wired 尺寸缩放（复刻 Pixaroma Image Resize） · §22 SFPauseLatent：latent 快照闸门（分段采样中间暂停） · §34 SFLoadImageBrowser 右键菜单：提示词复制与工作流载入（全链路复用零后端改动，2026-08） · §35 SFMaskFill：统一填充节点（合并 SFMaskedFill/SFMaskFillColor，falloff/skip 全局 + 条件显隐，2026-08） · §36 SFImageSceneSplit：镜头切分（硬切/黑白场/溶解 + 负索引/max_frames 首 N 帧 + LIST 全段，2026-08） · §37 SFImageCropExpand：出界裁剪/外绘预处理（复刻 YCNodes Load Image Crop Expand，源图磁盘持久化 + 拖拽冻结快照防飘移 + Browse 复用 Image Browser 弹窗选择器模式，2026-09） · §44 画布节点拖拽缩小外溢：computeSize 包装钳最小尺寸（双端 resize clamp 一处生效，onResize 是 legacy-only，2026-09） · §45 SFImageBrushMask：节点内画笔遮罩（复刻 YCNodes Load Image Brush Mask，磁盘持久化 + lean 注入 + 颜色误判修复，2026-09） · §51 SFImageBatchRange：批次区间切片（复刻 KJ GetImageRangeFromBatch，双路/-1/截断，2026-09） · §60 SFImageCropExpand 自定义比例预设持久化（user/sfnodes/crop_expand_presets.json 全局库 + Custom 弹窗改管理窗 + 复用 disk_state/valid_name，2026-09） · §62 SFPauseImage 水平翻转开关（复用闸门引擎 opt-in：已有快照就地镜像 temp PNG 使预览/下游/保存一致，无快照则捕获时 torch.flip；extraState 贯通 INJECT/PRUNE，2026-09） · §63 SFPauseImage 外载图（Design D：Load/Browse/拖放/粘贴落盘 input/ 并物化 temp 快照供 Continue 提交，Pause/Pass 仍走接线、加载不断线；独立 sf_pause_source.js 隔离重依赖，2026-09） · §64 SFInvertTrackData：SAM3 追踪数据反转（跨核心封闭类型：driving 只吃 track_data 故须位打包层取反 + 空帧/空 track 一致全选 + numpy 位序桩测试，2026-09） · §65 SFMaskToTrackData：MASK→SAM3_TRACK_DATA 桥接（第三方追踪器如 SeC 接 SCAIL2ColoredMask driving；ref 口本就吃 MASK 免桥接；复用核心 pack_masks + W 补 8 倍数 + 单对象约束，2026-09） · §66 SFMaskCache：遮罩磁盘缓存（lazy 输入命中即真跳过上游分割 + name/signature/source 哈希缓存键 + user/sfnodes/mask_cache 三件套与前端下拉，2026-09） · §67 SFTrackDataCache：SAM3_TRACK_DATA 层缓存（保留多对象/scores，不走 MASK 并集塌缩；缓存通用逻辑抽 sf_utils/cache_store.py + 前端 sf_cache_name_lib.js 单源，2026-09） · §69 画布节点拖拽释放兜底 + 按键守卫：SFImageCropExpand/SFImageBrushMask 截选框/笔触粘鼠标（根因=前端 processMouseUp 只对 node_over 回调且 stopPropagation；修复=sf_common installNodeReleaseGuard window capture + primaryButtonReleased 守卫，复用两节点，2026-09） · §71 官方原生 LoadImage/LoadImageMask 挂 Browse 按钮（复用 showImageBrowser 选择器模式 + applyNativeLoadImagePick 写回原生 image widget；combo 列表校验被 VALIDATE_INPUTS 形参跳过，output 注解值免 options 污染，2026-09） · §76 SFReferenceRegionNeutralize 参考区域中和（Krea2 洗图保留姿势/背景只换脸：ref_strength 全局旋钮无法空间解耦 + 图像/latent 域中和脸区等价且避开 attention 级 mask 的 flash/sage 与显存问题；blur/mean/fill + 羽化 + strength，配对 GeneratePreciseFaceMask 与 SFRegionalLoRA 脸区加强，2026-09） · §75 SFImageResizePlus size_mode 复刻原生 ResizeImageMaskNode（新增 倍率/长边/短边/倍数 4 模式，跳过冗余 width/height；目标尺寸纯函数收敛 resize_engine；scale to multiple 强制 cover 隐藏 method + divisible_by 退场（避免二次取整破坏倍数网格）；widgets_values 8→10→14 逐级 remap，2026-09） · §80 缓存节点名接入文本（SFTrackDataCache/SFMaskCache 可选 name_text forceInput 覆盖下拉（非空优先、单源解析、懒命中同源）+ Vue 前端 widget.disabled 须配 updateComputedDisabled + isSlotConnected 复用，2026-09） · §81 SF Track Data Add：track_data 并集叠加合成单身份 + 动态槽位（Subtract 4→20 静态 schema + 前端裁剪；复用 _exclusion_frames/_resize_frames；抽出 installConfiguredSlotRecovery 单源，combine/concat/track 三处共用；§81.5 add_to_track_data 必须继承输入 orig_size（追踪器方形工作网格 ≠ 真实宽高，否则 SAM3 Track to Mask 输出 1:1），2026-09） · §83 SFTrackDataMerge 单节点逐槽加减（动态槽 track_1..N + 每槽 DOM 行 `-`/`+` 模式切换替代 Subtract 串联 Add；等价先减后加、顺序无关 + add 无有效叠加不调用否则误塌单身份 + 模式经 properties/graphToPrompt 注入 hidden SlotModes + 前缀撞名 track_data 用 inputMatch/数字后缀排除，2026-09） · §85 SFCanvasSizePreset 全局自定义分辨率库（model 下拉 -- Custom -- 伪模型 "Custom Resolution" 独占自定义库 + combo 值 `WxH (name)` 编码 + 新路由 /api/sfnodes/canvas_size_custom + 双端归一化 + 跨节点同步，2026-09） · §88 SFTrackDataSlice：SAM3_TRACK_DATA 时间维切片（start 负值取尾/len=0 到尾/保留对象数与 orig_size；外部分段配套，纯逻辑 slice_track_data，2026-09） · §90 SFImageCropExpandBrushMask：出界裁剪 + 画笔遮罩合体（SFImageCropExpand × SFImageBrushMask 单节点：mask=扩展区∪笔触、Crop/Brush/Erase 三模式、笔触限源图坐标；全量共享提取 sf_crop_source/sf_crop_expand_ratios/sf_brush_tools + crop.load_src_rgb/_compose_expand(overlay=)/lean_key，2026-09）  · §91 SAM 菜单二期：合体节点接入 + 忙时熔断（comfy-aimdo 全局 file reader 非线程安全 → queue_busy() 409 熔断 + 同步路由封死入队竞态；前端共享 sf_brush_sam.js，2026-09）  · §92 画笔节点模式快捷键（B/E，合体节点 +C）：keyMap 泛化注册器 + 前端默认键位核查（纯单键占用表），2026-09  · §93 画笔节点 AI 菜单三期：人物部位（MediaPipe，核心抽 sf_utils/person_mask.py）/ YOLO（models/ultralytics 可选 ultralytics 依赖，白名单+纯 numpy 框掩码）/ 导入遮罩 / SAM 点·框选（coords/bbox，conditioning=None）/ 反选（笔刷 1-笔触，合体 扩展区∪(1-笔触)）；sf_brush_sam.js 泛化为 sf_brush_ai.js，2026-09  · §94 YOLO 菜单增强（多目录扫描含 models/yolo legacy / task 自检（segm 拒 detect）/ 类别过滤 classes / imgsz 640·960·1280；模型目录整理与持久化核查，2026-09）  · §95 反选遮罩面板按钮（TOOL_COL 插入 invert + INVERT_ON_COLOR 琥珀状态色，两节点共用 toggleInvert；教训：共享列定义增删项勿在测试写死行号，2026-09）  · §96 组合节点非 Crop 模式隐藏裁剪框（drawCropBox 门控，扩展区白提示保留；smoke 断言按尺寸/样式区分手柄与按钮，2026-09）  · §97 画布线条粗细全局设置（sfnodes.Canvas.FrameWidth/CursorWidth 拆分默认 1.0；sfFrameThin=max(0.5, W/2) 派生辅助细线；源图边界全模式保留致线宽不可作框判定，smoke 陷阱，2026-09） · §99 画笔节点 AI 识别结果随模式加减（复用 Brush/Erase 模式：Brush=添加、Eraser=减去识别区域，新增 fill_erase 打洞模式——mergeStrokes 单点改写 + 后端 rasterize 清 0 + 前端 destination-out 多边形，导入遮罩同样跟随，零新状态/UI，2026-09） · §101 画笔节点多边形套索（多次点选闭合填充：Poly 状态开关紧接 Erase，Brush=fill 添加 / Eraser=fill_erase 打洞；点首点/双击/Enter 闭合、Backspace 删点、Esc 取消（无顶点时关工具）；共享 web/sf_brush_poly.js + lib 纯几何，零后端改动复用 §99 的 fill/fill_erase；合体节点 Crop 下点击自动切 Brush、落点限源图内，MIN 高 300→320；顶点标记尺寸可调（sfnodes.Canvas.PolyVertexSize，默认 2）+ ON 绿状态色（POLY_ON_COLOR），2026-09） · §104 移除 insightface 依赖（人脸推理自研 ONNX 化：SCRFD/2d106/ArcFace 解码 + Umeyama 替代 skimage + insightface_utils→face_analysis 兼容层，模型/节点签名不变，2026-09） · §109 组合节点双列视觉分层（列头 RATIO/TOOLS + 组间分隔线 + Color/BCol→Fill/Pen 重命名 + 缩写按钮悬停底行中文说明；按钮位置不变、纯前端，MIN 高 320→340，2026-09） · §110 缓存节点 source 惰性化 + source_key 轻量键（SFTrackDataCache/SFMaskCache：source 转 lazy 避免命中判定整段解码；source_key 非空优先且命中不请求 source；命中带上游数据不重写 + safetensors 原子写 + 写盘日志，2026-09） · §111 缓存节点接口收口：source/signature 移除、source_key 成为唯一失效键（required 非空、空串报错；meta 字段 source_key 兼容读旧 source；迁移指引：视频源 filename+frame_count+尺寸文本键，图片源自备文本键，2026-09） · §112 组合节点源图翻转/旋转工具（第三列 ORIENT：FlipH/FlipV/RotL/RotR 源图整体联动、裁剪框与笔触随图重映射；纯前端落盘改写——orientState 纯函数 + orientSource canvas 重绘上传新源图，零后端改动/无需重启；MIN 宽 360→400，2026-09） · §113 组合节点扩展区遮罩开关 Ext（include_ext：面板列2 Invert 后新增 Ext，默认 ON=扩展区计入 mask；OFF 时 mask 只含笔触层、反选亦不带回扩展区、预览白提示不画；_compose_expand 加 include_ext 参数，MIN 高 340→360，2026-09） · §114 SFTrackDataToMask：track_data 指定帧输出遮罩（帧总数取 packed 首维/回退 n_frames，frame_index 负值取尾、越界报错；对象并集用公共 parse_object_indices——空=全部、越界/非法忽略，SFInvertTrackData 同步复用删本地副本；按 orig_size 双线性还原真实分辨率对齐核心 SAM3_TrackToMask，orig_size 缺失/非法报错不退回方形工作网格；空追踪/无有效对象全零，2026-09） |
| `nodes-lora.md` | LoRA / Civitai / Krea2 / Flux2 编码生态 | §5 Qwen3 无审查微调版 + TextGenerate：thinking 参数与思考链（COT） · §19 SFLoraStack：多行 LoRA 栈复刻（触发词/描述/封面/Civitai 查询/孤儿数据迁移） · §20 SFLoraStack：正交堆叠 ortho_gs（2026-08） · §21 Civitai 页面主体描述补充（curl_cffi / __NEXT_DATA__ 与 Cloudflare 拦截） · §25 SFRegionalLoRA：多区域角色 LoRA（token 网格注入与匹配诊断） · §28 SFStylesSelector：风格选择器复刻（Easy-Use stylesSelector） · §31 Krea2 预设管理：SFImageInterrogator / SFKrea2SystemPrompt（内置 + 用户覆盖 + 墓碑复位） · §32 SF Load Diffusion Model：信息面板跨域复用（别名路由域分派 + ctx.api 整束注入，2026-08） · §33 TextEncodeKrea2 视觉通路机制（Qwen3-VL 单帧绑定 / min_pixels 兜底 / RGBA 预乘时序，2026-08） · §34 SFLoraStack 复合预设：LoRA 顺序/强度 + 正向提示词（与 triggers 分离，2026-08） · §35 SFImageInterrogator seed 与 control_after_generate：显式声明与 widgets_values 位置敏感自愈（2026-08） · §36 SFLoraStack 触发词勾选全局默认（新建行默认值，工作流覆盖，2026-08） · §38 SFImageInterrogator 三模态可选 + user_prompt 文本框化（对齐原生 Generate Text，2026-08） · §39 SFWanWindowLoRA 逐窗口位置 LoRA（EVALUATE 回调 patch-swap，零 GPU 常驻，2026-09） · §39.6 SFWanWindowPlanner 只算不跑的映射表 · §39.6.2 SFWanWindowPlanner 参数透传输出（2026-09） · §39.9 SFLoraStack 第5输出preset_export与model可选（2026-09） · §39.10 SFLoraStack 预设菜单出界修复（删内联列表，取用走大面板，2026-09） · §39.11 SFWanWindowLoRA 的 GGUF 双通道（GGMLOps 量化层走张量 patches + 上游 official 共存回归，2026-09） · §56 官方加载器 info 图标前端挂载（NODE_TYPES 双挂零核心改动，2026-09） · §59 SFPainterFluxImageEdit：Flux2 图编一体编码（复刻 PainterFluxImageEdit，动态槽位 + 批次不复制 conditioning/step16/遮罩澄清 + negative/method/起点/VL 开关 + collect_indexed 收敛，2026-09） · §61 全局 combo 树形下拉（multi_lora_tree 门控放宽到全部系统下拉 + 无路径早退/分组头排除，2026-09） · §73 Krea2 参考图编辑 5 节点复刻（摆脱 ComfyUI-EditUtils：核心 0.35 原生 Krea2 ref 通路但 default_ref_method=None 须补丁；编码复用 qwen_edit + 逐行移植 Krea2EditApply 补丁/KV 缓存/ref_strength；SF 前缀 + 工作流 JSON 改写，2026-09） · §75 SFImageInterrogatorAPI（LLM API 版图像反推：sf_utils/llm_client 单源 + 共享 sfnodes.LLM.* 设置后端读 comfy.settings.json + 复用反推预设库/前端双 class + 单帧 base64 视觉请求 + seed/send_seed 条件透传 + 共享 LRU 缓存（CacheEnabled 开关），2026-09） |
| `apps.md` | 无节点面板应用 | §10 SF Workflows：工作流面板（复刻 Pixaroma Workflows） · §30 SF LoRA 浏览器：工具栏应用 + 信息面板宿主 ctx 适配（浏览全部 LoRA 并编辑信息） |
| `nodes-video.md` | 视频与视频生成节点 | §72 SCAIL-2 四节点复刻（ComfyUI-SCAIL2-Easy：Fit Video / Reference Pack / Reference SAM Builder / Simple Video，复用核心 WanSCAILToVideo·SCAIL2ColoredMask·SAM3_VideoTrack·context_windows，2026-09） · §77 SAM3 视觉点选追踪与 track_data 排除（SFSAM3PointTrack 锚帧检测+前补空帧回全长 / SFTrackDataSubtract 逐对象相减保留对象数，纯逻辑 sf_utils/track_data_ops.py；驱动遮罩排男/阴茎/精液，2026-09） · §78 SCAIL-2 上下文窗口参数对齐原生 WanContextWindowsManual（context_schedule/context_stride/closed_loop/freenoise 可选项、freenoise 补挂 sampler wrapper、stride 仅 uniform 系与 closed_loop 仅 looped 前端条件显隐、retain_first_frame 对 SCAIL-2 无效；核心已原生切 SCAIL 蒙版故旧键名 patch 为兼容垫片勿删；单图负向条件与核心一致共用 ref，2026-09） · §82 SCAIL-2 预处理 O(T) 内存分块（运行时补丁 _render_colored_masks/_extract_mask_to_28ch 分块 + WanSCAILToVideo.execute 参考蒙版裁剪；逐元素等价依据与残留 O(T) 输出、设置 sfnodes.SCAIL2Mem.{Enabled,ChunkFrames,HalfPrecision}，2026-09） · §87 SCAIL-2 外部分段处理（VHS skip/cap 分段 + SFForLoop 循环 + previous_frames 外锚接续 + SFTrackDataSlice 段内 track + SFVideoConcat 文件级 remux 合并与音频注入；段长 461=5+76×6 公式与 RAM O(段长) 工作集，2026-09） · §98 SFSAM3ReanchorTrack 指定帧重锚追踪（锚帧分段调用原生 SAM3_VideoTrack 硬重置记忆 + clip/prompts 逐行提示词重检测/空行回退 conditioning + 首锚 initial_mask；纯逻辑 concat_track_data_segments 位或塌单身份拼回全长 + node_result 公共解包 + initial_mask 批语义（第 i 张按序对应第 i 段；多余丢弃/不足或全零回退文本，取代 initial_mask_always 开关）+ anchor_interval 间隔锚帧（与列表合并去重）+ 提示词粘性继承（缺失行继承/空行重置，单行全段生效），2026-09） · §100 SCAIL2Mem 设置语义收口（Enabled 主开关：关闭后分块/参考蒙版裁剪/f16 全部停用；新增 numpy 代理分块逐元素等价回归 tests/test_scail2_mem.py；read_comfy_settings 加 (path,mtime_ns,size) 读盘缓存，2026-09） · §105 SFWanMotionBoost：Wan I2V 慢动作增强（委托原生 WanImageToVideo 后处理 concat 占位帧运动放大 + 逐通道保均值色彩保护，可关为 Painter 原版跨通道复现档；去掉 reference_latents placebo，2026-09） |

## 维护规则

- 新增经验：按主题追加到对应文件的**下一个全局 §N（编号只增不复用、不重排）**，并在上表加行；**不再在 `AGENTS.md` 增补索引行**（AGENTS 只引用本表）。
- 最小事实订正：失效的文件/函数/包/API 名、断链引用、明显错别字可就地修正；**不得改写历史结论与论证**；拿不准的过时内容用带日期的补注而非改原文。
- 取代机制：机制被后续方案取代时，新经验仍开新 §N，并在旧节开头加一行 `> ⚠️ 已被 §N 取代（YYYY-MM）`（新节注明取代关系，双向指针）。
- **新类别且现有主题均不适配时可新建主题文件**：英文短名对齐节点族/领域（如 nodes-video.md）、标题行注明所含 § 章节、本表加行——勿把无关章节塞进既有文件（检索会整文件加载）。
- 跨文件引用一律写 §N（不写相对路径），由本表定位；各文件开头注明所含章节。
- 章节内小节号（如 §19.7）指「§19 文件内的 ### 7.」，随所属章节走。

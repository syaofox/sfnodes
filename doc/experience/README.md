# 经验归档索引（experience）

> 本目录由原单文件 `doc/experience.md` 拆分（2026-08）：**全局章节号 §N 保持不变**，AGENTS.md 经验摘要与文内互引的 `§N` 由下表定位到具体文件，按需读取避免整档加载。
> 本目录归档 AGENTS.md 精简（2026-08）时删除的具体机制与踩坑经验；主文档只保留通用约束与每类机制的结论索引（见 AGENTS.md「经验摘要」）。
> 内容基于当时代码/前端版本（comfyui_frontend_package 1.x，Vue 重构后，版本号会随升级变化，以容器内 `pip show comfyui-frontend-package` 为准，各节另有标注），可能随版本升级过时，使用时结合代码核实。

## 文件与章节映射

| 文件 | 主题 | 章节 |
|---|---|---|
| `platform.md` | 平台机制（ComfyUI 前后端通用） | §1 ComfyUI 后端机制（循环/图展开） · §2 ComfyUI 前端机制（经验总结，含 §11 画布多选宽度对齐、§13 槽点/槽名基线错位全局行为、§14 SF Memory 画布菜单清理 VRAM/RAM、§15 画布菜单聚合 📦 SF Menu、§16 主题令牌层：DOM UI 跟随 Color Palette 明暗） |
| `patterns.md` | 横切模式与修复批次 | §3 静态检查脚本经验（AST 对比踩坑） · §4 动态 combo 校验与工作流绑定状态（widget 数据载体） · §17 复刻节点去重：sf_common.js / disk_state.py 公共模块收敛与踩坑 · §26 前端架构治理（2026-08）：工具收敛 / 弹层三件套 / 纯模块边界 · §27 2026-08 健壮性修复批次：表达式防御 / ReDoS 交替型 / 路径净化 / 双端镜像补缺 · §39 COLOR 输入类型被 Vue 前端内置 widget 收编（default 写 hex 不写数组，2026-09） · §40 number widget 整数/小数输入切换（SFNumber 改 options 不换类型 + PERCENT ÷100 百分数语义与切档换算 + 输出单槽 any 化，2026-09） · §41 combo→输出槽类型前端改型（SFConvert Anything：setSlotType 元素替换 + configure 恢复补原件缺陷，2026-09） · §43 灵活 optional schema 与 Any Switch 复刻（`__contains__` 恒 True 放行动态输入 + installDynamicSlots 槽位 + any_pack 着色三件套，2026-09） · §49 固定类型动态多槽 SFConditioningCombine（灵活 schema 类型参数化 + 固定类型免着色 + 按链接数补齐/回收，2026-09） · §50 SFConditioningConcat 多路拼接（slot1=to 逐条保留 + from 各取首条 + 缺 base 抛错 + stub-torch 测试，2026-09） · §52 SFUniversalSlider 万能滑条 Canvas 复刻规范化（去全局补丁/相对导入修正/popup 收敛/RETURN_NAMES 静态折中，2026-09） · §53 SFBooleanSwitch 布尔开关 Canvas 复刻（TOGGLE 常量同源/双击标签/properties 键改名，2026-09） · §54 SFIgnoreGroups 忽略多组编组开关复刻（DOM 面板/三模式/graph.change 单例/轮询清理/锚点组员判定 + 两趟写入，2026-09） · §55 SF 注释便签复刻（原版纯前端架构 1:1/全局补丁守卫/画布菜单入口，2026-09） · §57 纯函数单源收敛与测试桩同步（import 别名零改动/语义差参数化/测试桩三形态，2026-09） · §58 第二批收敛（守卫单源/前缀清洗/原子写盘/kind 参数化 + 删文件联动清单，2026-09） · §68 PointsEditor 底图刷新（跨包解析上游链 + 复用 KJNodes editor.processImage 零执行持久化 + 纯 lib 描述符 + 分段偏移/标注帧感知 seek，2026-09） · §69 手动分段 SeC 管线（单次加载 N 段切片 + 每段首帧标注 + SFMaskBatch 合并 + 男性/精液相减，2026-09） · §70 前端 INT 缺 max 默认 2048（第三方节点数值上限规避：定义阶段抬 spec.max，2026-09） |
| `nodes-text.md` | 文本与提示词节点 | §6 SFPromptTags：@tag 展开注入 / Picks 游标 / 全屏编辑器 / 中文与拼音（复刻 Pixaroma Prompt） · §7 SFPauseText：prompt 剪枝闸门（复刻 Pixaroma Pause Text） · §14 SFTextFindReplace：查找替换双端镜像（复刻 Pixaroma Find & Replace） · §15 SFValueDropdown：值下拉与输出点对齐（复刻 Pixaroma Dropdown） · §16 SFPromptReader：PNG/视频元数据提示词恢复（复刻 Pixaroma Prompt Reader） · §18 SFLoadImagesPath 目录切换：两源 + 渐进式浏览 + popup 下拉 · §23 SFPromptList：行号编辑器与 wrap 镜像测量 · §24 SFPromptStack：动态 Prompt 列表与行高拖拽 · §29 SFPromptPreset：十一分类组合预设（正交原则 / 随机机制 / LLM 优化链路） · §36 SFLongTextToList：长文本分割转列表（复刻 ComfyUI_Lam LongTextToList） · §37 SFTextListAffix：列表前后缀（输入列表加前后缀） · §42 SFTextPreset 工作流绑定预设→全局持久化（user/sfnodes/text_presets.json 真源 + 工作流回退兼容 + API 失败降级，2026-09） · §46 SFIDClothingSelector 证件照服装单选（复刻孤海画廊 + 复用 styles JSON 生态 id_ 子集 + 草稿优先 + IMAGE 模板图） · §47 SFCharacterSelect 角色三分镜单选（脸/半身/全身三路独立 IMAGE + character_ JSON 生态 + 新双路由） · §48 SFCharacterSelect v2 角色内多选 batch（破兼容 4→3 路 + 双读兼容 + 首图归一） · §74 SFPauseText 中⇄EN 翻译按钮（LLM API 经 /api/sfnodes/translate 后端代理 + sfnodes.Translate.* 设置 + thinking 仅 DeepSeek） |
| `nodes-image.md` | 图片 / 遮罩 / latent 节点 | §8 SFPauseImage：快照闸门与预览保存（复刻 Pixaroma Pause Image） · §9 SFPauseMask：遮罩快照闸门（Pixaroma Pause Mask 同构扩展） · §11 SFImageCrop/SFImageUncrop：可视化裁剪与贴回（复刻 Pixaroma Crop/Uncrop） · §12 SFImageOutpaint/Stitch：外绘填充与原始图贴回（复刻 Pixaroma Outpaint） · §13 SFImageResize：wired 尺寸缩放（复刻 Pixaroma Image Resize） · §22 SFPauseLatent：latent 快照闸门（分段采样中间暂停） · §34 SFLoadImageBrowser 右键菜单：提示词复制与工作流载入（全链路复用零后端改动，2026-08） · §35 SFMaskFill：统一填充节点（合并 SFMaskedFill/SFMaskFillColor，falloff/skip 全局 + 条件显隐，2026-08） · §36 SFImageSceneSplit：镜头切分（硬切/黑白场/溶解 + 负索引/max_frames 首 N 帧 + LIST 全段，2026-08） · §37 SFImageCropExpand：出界裁剪/外绘预处理（复刻 YCNodes Load Image Crop Expand，源图磁盘持久化 + 拖拽冻结快照防飘移 + Browse 复用 Image Browser 弹窗选择器模式，2026-09） · §44 画布节点拖拽缩小外溢：computeSize 包装钳最小尺寸（双端 resize clamp 一处生效，onResize 是 legacy-only，2026-09） · §45 SFImageBrushMask：节点内画笔遮罩（复刻 YCNodes Load Image Brush Mask，磁盘持久化 + lean 注入 + 颜色误判修复，2026-09） · §51 SFImageBatchRange：批次区间切片（复刻 KJ GetImageRangeFromBatch，双路/-1/截断，2026-09） · §60 SFImageCropExpand 自定义比例预设持久化（user/sfnodes/crop_expand_presets.json 全局库 + Custom 弹窗改管理窗 + 复用 disk_state/valid_name，2026-09） · §62 SFPauseImage 水平翻转开关（复用闸门引擎 opt-in：已有快照就地镜像 temp PNG 使预览/下游/保存一致，无快照则捕获时 torch.flip；extraState 贯通 INJECT/PRUNE，2026-09） · §63 SFPauseImage 外载图（Design D：Load/Browse/拖放/粘贴落盘 input/ 并物化 temp 快照供 Continue 提交，Pause/Pass 仍走接线、加载不断线；独立 sf_pause_source.js 隔离重依赖，2026-09） · §64 SFInvertTrackData：SAM3 追踪数据反转（跨核心封闭类型：driving 只吃 track_data 故须位打包层取反 + 空帧/空 track 一致全选 + numpy 位序桩测试，2026-09） · §65 SFMaskToTrackData：MASK→SAM3_TRACK_DATA 桥接（第三方追踪器如 SeC 接 SCAIL2ColoredMask driving；ref 口本就吃 MASK 免桥接；复用核心 pack_masks + W 补 8 倍数 + 单对象约束，2026-09） · §66 SFMaskCache：遮罩磁盘缓存（lazy 输入命中即真跳过上游分割 + name/signature/source 哈希缓存键 + user/sfnodes/mask_cache 三件套与前端下拉，2026-09） · §67 SFTrackDataCache：SAM3_TRACK_DATA 层缓存（保留多对象/scores，不走 MASK 并集塌缩；缓存通用逻辑抽 sf_utils/cache_store.py + 前端 sf_cache_name_lib.js 单源，2026-09） · §69 画布节点拖拽释放兜底 + 按键守卫：SFImageCropExpand/SFImageBrushMask 截选框/笔触粘鼠标（根因=前端 processMouseUp 只对 node_over 回调且 stopPropagation；修复=sf_common installNodeReleaseGuard window capture + primaryButtonReleased 守卫，复用两节点，2026-09） · §71 官方原生 LoadImage/LoadImageMask 挂 Browse 按钮（复用 showImageBrowser 选择器模式 + applyNativeLoadImagePick 写回原生 image widget；combo 列表校验被 VALIDATE_INPUTS 形参跳过，output 注解值免 options 污染，2026-09） · §75 SFImageResizePlus size_mode 复刻原生 ResizeImageMaskNode（新增 倍率/长边/短边/倍数 4 模式，跳过冗余 width/height；目标尺寸纯函数收敛 resize_engine；scale to multiple 强制 cover 隐藏 method + divisible_by 退场（避免二次取整破坏倍数网格）；widgets_values 8→10→14 逐级 remap，2026-09）|
| `nodes-lora.md` | LoRA / Civitai / Krea2 / Flux2 编码生态 | §5 Qwen3 无审查微调版 + TextGenerate：thinking 参数与思考链（COT） · §19 SFLoraStack：多行 LoRA 栈复刻（触发词/描述/封面/Civitai 查询/孤儿数据迁移） · §20 SFLoraStack：正交堆叠 ortho_gs（2026-08） · §21 Civitai 页面主体描述补充（curl_cffi / __NEXT_DATA__ 与 Cloudflare 拦截） · §25 SFRegionalLoRA：多区域角色 LoRA（token 网格注入与匹配诊断） · §28 SFStylesSelector：风格选择器复刻（Easy-Use stylesSelector） · §31 Krea2 预设管理：SFImageInterrogator / SFKrea2SystemPrompt（内置 + 用户覆盖 + 墓碑复位） · §32 SF Load Diffusion Model：信息面板跨域复用（别名路由域分派 + ctx.api 整束注入，2026-08） · §33 TextEncodeKrea2 视觉通路机制（Qwen3-VL 单帧绑定 / min_pixels 兜底 / RGBA 预乘时序，2026-08） · §34 SFLoraStack 复合预设：LoRA 顺序/强度 + 正向提示词（与 triggers 分离，2026-08） · §35 SFImageInterrogator seed 与 control_after_generate：显式声明与 widgets_values 位置敏感自愈（2026-08） · §36 SFLoraStack 触发词勾选全局默认（新建行默认值，工作流覆盖，2026-08） · §38 SFImageInterrogator 三模态可选 + user_prompt 文本框化（对齐原生 Generate Text，2026-08） · §39 SFWanWindowLoRA 逐窗口位置 LoRA（EVALUATE 回调 patch-swap，零 GPU 常驻，2026-09） · §39.6 SFWanWindowPlanner 只算不跑的映射表 · §39.9 SFLoraStack 第5输出preset_export与model可选（2026-09） · §39.10 SFLoraStack 预设菜单出界修复（删内联列表，取用走大面板，2026-09） · §39.11 SFWanWindowLoRA 的 GGUF 双通道（GGMLOps 量化层走张量 patches + 上游 official 共存回归，2026-09） · §56 官方加载器 info 图标前端挂载（NODE_TYPES 双挂零核心改动，2026-09） · §59 SFPainterFluxImageEdit：Flux2 图编一体编码（复刻 PainterFluxImageEdit，动态槽位 + 批次不复制 conditioning/step16/遮罩澄清 + negative/method/起点/VL 开关 + collect_indexed 收敛，2026-09） · §61 全局 combo 树形下拉（multi_lora_tree 门控放宽到全部系统下拉 + 无路径早退/分组头排除，2026-09） · §73 Krea2 参考图编辑 5 节点复刻（摆脱 ComfyUI-EditUtils：核心 0.35 原生 Krea2 ref 通路但 default_ref_method=None 须补丁；编码复用 qwen_edit + 逐行移植 Krea2EditApply 补丁/KV 缓存/ref_strength；SF 前缀 + 工作流 JSON 改写，2026-09） · §75 SFImageInterrogatorAPI（LLM API 版图像反推：sf_utils/llm_client 单源 + 共享 sfnodes.LLM.* 设置后端读 comfy.settings.json + 复用反推预设库/前端双 class + 单帧 base64 视觉请求 + seed/send_seed 条件透传 + 共享 LRU 缓存（CacheEnabled 开关），2026-09） |
| `apps.md` | 无节点面板应用 | §10 SF Workflows：工作流面板（复刻 Pixaroma Workflows） · §30 SF LoRA 浏览器：工具栏应用 + 信息面板宿主 ctx 适配（浏览全部 LoRA 并编辑信息） |
| `nodes-video.md` | 视频与视频生成节点 | §72 SCAIL-2 四节点复刻（ComfyUI-SCAIL2-Easy：Fit Video / Reference Pack / Reference SAM Builder / Simple Video，复用核心 WanSCAILToVideo·SCAIL2ColoredMask·SAM3_VideoTrack·context_windows，2026-09） |

## 维护规则

- 新增经验：按主题追加到对应文件的**下一个全局 §N**（只增不改不重排），并在上表加行；同时在 `AGENTS.md` 经验摘要增补/更新索引行。**新类别且现有主题均不适配时可新建主题文件**：英文短名对齐节点族/领域（如未来的 nodes-video.md）、标题行注明所含 § 章节、本表加行——勿把无关章节塞进既有文件（检索会整文件加载）。
- 跨文件引用一律写 §N（不写相对路径），由本表定位；各文件开头注明所含章节。
- 章节内小节号（如 §19.7）指「§19 文件内的 ### 7.」，随所属章节走。
